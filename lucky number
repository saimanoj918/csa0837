def lucky_numbers_upto(n):
    numbers=list(range(1,n+1))
    i=1
    while i<len(numbers):
        step=numbers[i]
        del numbers[step -1::step]
        i+=1
    return numbers
print("lucky numbers up to 50 are:")
print(lucky_numbers_upto(50))
